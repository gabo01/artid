{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug backup",
            "cwd": "${workspaceRoot}",
            "env": {"PWD": "${workspaceRoot}"},
            "args": ["--backtrace", "backup", "--dir", "${workspaceRoot}/debug/backup"],
            "preLaunchTask": "builder",
            "sourceLanguages": ["rust"],
            "program": "${workspaceRoot}/target/debug/artid"
        }
    ]
}
